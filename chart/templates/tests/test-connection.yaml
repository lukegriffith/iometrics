apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "iometrics.fullname" . }}-test-connection"
  labels:
    {{- include "iometrics.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test-success
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "iometrics.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
